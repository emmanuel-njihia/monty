#include "monty.h"

/**
 * lifo - add in stack to lifo changes
 * @stack: NULL
 * @line_number: 0
 * Return: nothing, void
 */

void lifo(stack_t **stack, unsigned int line_number)

{
	(void) stack;
	(void) line_number;

	element->IFO = 0;
}

/**
 * fifo - add in stack to fifo changes
 * @stack: NULL
 * @line_number: 0
 * Return: nothing, void
 */

void fifo(stack_t **stack, unsigned int line_number)

{
	(void) stack;
	(void) line_number;

	element->IFO = 1;
}
