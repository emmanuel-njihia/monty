#include "monty.h"

/**
 * nop - doesn't do anything
 * @stack: ptr to stop of stack
 * @line_number: number line been executed
 * Return: void
 */

void nop(stack_t **stack, unsigned int line_number)

{
	(void) stack;
	(void) line_number;
}
